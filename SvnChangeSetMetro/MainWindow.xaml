<Controls:MetroWindow x:Class="SvnChangeSetMetro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="ChangeSet Maker" Height="600" Width="800" ShowIconOnTitleBar="True">

    <Window.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="reposTemplate">
                <Label Content="{Binding XPath=Name}"/>
            </DataTemplate>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Colours.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/Blue.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/BaseLight.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="64" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Margin="10,10,10,10" VerticalAlignment="Center">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="Repository Path" VerticalAlignment="Center"></TextBlock>
                <TextBox Name="textBoxRepoPath" Margin="5,0,0,0" HorizontalAlignment="Stretch" MinWidth="260"></TextBox>
                <Button DockPanel.Dock="Right" Name="buttonAddRepo" Width="32" Content="+" HorizontalAlignment="Right"></Button>
            </DockPanel>
        </StackPanel>
        <ListBox Grid.Row="1" Margin="12,0,10,12" Name="listViewRepos"
                 ItemsSource="{Binding}"
                 ItemTemplate="{StaticResource reposTemplate}"
                 IsSynchronizedWithCurrentItem="True"
                 Visibility="Visible" SelectionMode="Single">
        </ListBox>
                  
        <ListBox Grid.Column="1" Grid.Row="1" Margin="12,0,10,12" Name="listViewChanges" />

        <Grid.DataContext>
            <XmlDataProvider x:Name="ReposData" Source="Repos.xml" XPath="Repositories/Repo" />
        </Grid.DataContext>
    </Grid>
</Controls:MetroWindow>
